# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'

target 'Estimote Stickers' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Estimote Stickers
  pod 'EstimoteSDK'
  pod 'SnapKit', '~> 0.22.0'
  pod 'Alamofire', '~> 3.5.1'
  pod 'PromiseKit', '~> 3.5'

  target 'Estimote StickersTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Estimote StickersUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
